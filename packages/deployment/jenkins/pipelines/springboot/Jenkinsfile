pipeline {
    agent any

    stages {
        stage('Backend Spring Boot Tests') {
            steps {
                dir('packages/java/procureiq-springboot') {
                    sh 'mvn test'
                }
            }
        }
    }
}
